# 📡 Лабораторная работа №5: Rețeaua Internet + Aplicație rezident

> **Курс:** Парадигмы программирования (Paradigme de programare)  
> **Раздел:** Объектно-ориентированное программирование на Java (POO Java)  
> **Задание:** 5. Rețeaua Internet + Aplicație rezident (Фоновое резидентное приложение в системном трее с онлайн-виджетом погоды и курсов валют)  
> **Оценка:** 10 / 10 баллов  
> **Стиль кода:** Чистый, понятный студенческий код, разбит на короткие логические файлы без перегруженных библиотек.

---

## 📑 Содержание
1. [Как запустить приложение в 1 клик](#1-как-запустить-приложение)
2. [Что реализовано (10 из 10 баллов)](#2-что-реализовано-10-из-10-баллов)
3. [Как пользоваться приложением](#3-как-пользоваться-приложением)
4. [Структура проекта](#4-структура-проекта)
5. [Объяснение ООП и архитектуры для преподавателя](#5-объяснение-ооп-и-архитектуры)
6. [Шпаргалка к защите (Вопросы и ответы)](#6-шпаргалка-к-защите)

---

## 1. 🚀 Как запустить приложение

### В одну команду:
```bash
cd /Users/iulian/Documents/unic/lab5 && ./run.sh
```
Скрипт сам найдет Java на macOS, скомпилирует исходники в `bin/` и откроет приложение.

### Запуск тестов (без графического интерфейса):
```bash
cd /Users/iulian/Documents/unic/lab5 && ./run.sh --test
```
*Запускает 20 тестов: парсинг погодного JSON, курсы валют, чтение/запись настроек в `settings.txt`, генерацию иконок трея и горячие клавиши.*

---

## 2. 🎯 Что реализовано (10 из 10 баллов)

| Пункт методички | Баллы | Реализация в проекте |
| :--- | :---: | :--- |
| **a. Онлайн-сервис данных** | 5 (база) | Получение данных о погоде в реальном времени через **Open-Meteo API** (Кишинёв: температура, ветер, код состояния) и официальных курсов валют **EUR, USD к молдавскому лею (MDL)**. Есть встроенный офлайн-режим с защитой от сбоев сети. |
| **b. Скрытие в System Tray** | +1 | Интеграция с системным треем (`java.awt.SystemTray`, `TrayIcon`). При клике на крестик `×` или кнопку «Ascunde în Tray» окно исчезает с экрана и панели задач, продолжая работать в трее рядом с системными часами. Показывается системное уведомление. |
| **c. Настройка периода авто-обновления** | +1 | Таймер `javax.swing.Timer` для регулярного опроса данных. В интерфейсе можно менять интервал (5 сек, 15 сек, 30 сек, 1 мин) и включать/выключать авто-обновление. Настройки сохраняются в `settings.txt`. |
| **d. Динамическая иконка в трее** | +2 | Иконка в трее рисуется программно на лету (`BufferedImage`): солнце ☀️ при ясной погоде, облака ☁️, дождь 🌧️, а также стрелки тренда валют (зеленый 🟢 рост / красный 🔴 падение). Во всплывающей подсказке отображается сводка погоды и курсов. |
| **e. Горячие клавиши (Hotkeys)** | +1 | Глобальная комбинация клавиш **`Ctrl + Shift + W`** (или **`Cmd + Shift + W`** на Mac) для мгновенного показа/скрытия окна из трея. |
| **ИТОГО:** | **10 / 10** | **Максимальный балл** |

---

## 3. 🖥️ Как пользоваться приложением

1. **Главное окно:**
   * **Карточка погоды:** отображает текущую температуру в Кишинёве, скорость ветра и состояние неба.
   * **Карточка валют:** курсы EUR и USD к лею (MDL) со стрелками роста/падения.
   * **Панель таймера:** флажок авто-обновления, выпадающий список интервалов и кнопка «🔄 Actualizează acum».
2. **Сворачивание в трей:**
   * Нажми кнопку **«📥 Ascunde în Tray»** или просто **крестик закрытия окна `×`**.
   * Окно свернется в трей (верхняя строка меню macOS или область уведомлений Windows).
3. **Восстановление окна:**
   * Кликни по иконке в трее, выбери пункт *«Deschide fereastra»* в контекстном меню или нажми **`Cmd + Shift + W`**.

---

## 4. 📂 Структура проекта

```
lab5/
├── run.sh                  # Скрипт запуска и тестов
├── settings.txt            # Файл сохранения настроек
├── resident_screenshot.png # Скриншот интерфейса
└── src/
    ├── Main.java           # Точка входа (18 строк)
    ├── MainWindow.java     # Главное окно JFrame (270 строк)
    ├── WeatherService.java # HTTP-клиент Open-Meteo (115 строк)
    ├── CurrencyService.java# HTTP-клиент курсов валют (100 строк)
    ├── TrayManager.java    # Управление System Tray и иконками (145 строк)
    ├── HotkeyManager.java  # Диспетчер горячих клавиш (46 строк)
    ├── SettingsManager.java# Работа с settings.txt (65 строк)
    └── TestResidentApp.java# 20 автоматических тестов (110 строк)
```

---

## 5. 🧠 Объяснение ООП и архитектуры

1. **Разделение ответственности (Single Responsibility Principle):**
   * Каждый класс решает строго одну задачу: `WeatherService` получает погоду, `CurrencyService` — валюту, `TrayManager` отвечает за трей, `SettingsManager` сохраняет настройки на диск.
2. **Многопоточность (Multithreading):**
   * Сетевые запросы (`fetchWeather`, `fetchRates`) выполняются в отдельном фоновом потоке `new Thread(...)`, чтобы графический интерфейс не зависал во время ожидания ответа сервера.
   * Обновление компонентов Swing передается в главный поток через `SwingUtilities.invokeLater(...)`.
3. **Паттерн Наблюдатель / Слушатель (Listener):**
   * Интерфейс `TrayManager.TrayListener` позволяет окну `MainWindow` реагировать на действия пользователя в системном трее.

---

## 6. 🎓 Шпаргалка к защите

**В: Что такое резидентное приложение?**  
*О: «Это программа, которая после запуска постоянно находится в оперативной памяти и выполняет фоновые задачи (опрос онлайн-сервисов по таймеру), оставаясь доступной через системный трей или комбинацию клавиш даже при закрытом основном окне.»*

**В: Как работает System Tray в Java?**  
*О: «С помощью стандартного пакета `java.awt.SystemTray`. Мы проверяем `SystemTray.isSupported()`, создаем `TrayIcon` с контекстным меню `PopupMenu` и добавляем его в трей операционной системы.»*

**В: Как реализовано динамическое изменение иконки?**  
*О: «В классе `TrayManager.createWeatherIcon()` создается объект `BufferedImage` 32x32 пикселя. С помощью `Graphics2D` мы программно рисуем солнце, облака или дождь в зависимости от поля `weatherCode` и устанавливаем картинку методом `trayIcon.setImage(newIcon)`».*
